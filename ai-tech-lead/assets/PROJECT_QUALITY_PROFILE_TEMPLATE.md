# Project Quality Profile

将本文件内容整理后放入项目 `AGENTS.md`、`CLAUDE.md` 或团队规则中。

## 项目概览

- 技术栈：
- 包管理器：
- 主要入口：
- 核心模块：
- 高风险模块：
- Git／非 Git 基线与快照位置：
- 允许的网络、凭证和生产操作：

## 环境与安装

```text
<安装依赖>
<初始化环境>
```

## 构建与启动

```text
<构建命令>
<开发启动命令>
<生产模拟命令>
```

## 质量命令

```text
<格式检查>
<Lint>
<类型检查>
<定向测试格式>
<模块测试>
<全量测试>
<E2E／运行验证>
```

## 测试选择规则

- 修改 `X` 时必须运行：
- 修改公共接口时必须运行：
- 修改数据／迁移时必须运行：
- 修改 UI 时必须运行：

## 架构与编码规则

- 分层：
- 依赖方向：
- 错误处理：
- 日志：
- 数据访问：
- API／事件兼容：

## 禁止事项

- 禁止修改：
- 禁止命令：
- 禁止新增依赖：
- 禁止接触的凭证／数据：
- 需要人工批准的操作：
- 审批人、有效期和证据存放位置：
- 默认禁止的外部数据出口：

## Git 与隔离

- 分支规则：
- worktree 规则：
- 提交规则：
- 不得覆盖的用户本地改动：
- Agent 超时、崩溃和重试上限：
- 外部 Agent 前置检查：Provider／版本、实际 YOLO／免确认权限证据、最高／生效 thinking 证据；缺证据时 `BLOCKED`

## Definition of Done

- [ ] 验收标准对应实现和测试
- [ ] 必要构建、检查、测试通过
- [ ] 最终 Diff 已审查
- [ ] 兼容性、安全和回滚已考虑
- [ ] 未验证项已披露
